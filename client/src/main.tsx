import React from 'react'; import ReactDOM from 'react-dom/client'; import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import './styles.css'; import Navbar from './components/Navbar'; import { HomePage, LoginPage, PostPage, ProfilePage, RegisterPage, SettingsPage, EditorPage } from './pages'; import { useAuthStore } from './store/authStore';
const qc=new QueryClient();
const Protected=({children}:{children:React.ReactNode})=>useAuthStore.getState().token?<>{children}</>:<Navigate to='/login'/>;
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={qc}><BrowserRouter><Navbar/><Routes><Route path='/' element={<HomePage/>}/><Route path='/login' element={<LoginPage/>}/><Route path='/register' element={<RegisterPage/>}/><Route path='/posts/:slug' element={<PostPage/>}/><Route path='/editor' element={<Protected><EditorPage/></Protected>}/><Route path='/editor/:slug' element={<Protected><EditorPage/></Protected>}/><Route path='/profile/:username' element={<ProfilePage/>}/><Route path='/settings' element={<Protected><SettingsPage/></Protected>}/></Routes></BrowserRouter></QueryClientProvider></React.StrictMode>);
